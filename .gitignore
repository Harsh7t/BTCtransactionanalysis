.venv/
node_modules/
__pycache__/
*.pyc
.DS_Store
data/samples/*.csv
data/samples/*.jsonl
data/samples/*.xml
data/samples/*_truth/
data/uploads/
data/exports/
data/case.duckdb
data/train.duckdb
wheels/

# The built UI bundle IS committed: `make serve` serves it, and a teammate who
# clones without it gets a blank page. 1.5 MB, rebuilt by `make ui`.

# Real external datasets: not ours to redistribute, and large.
# Elliptic (Kaggle, ellipticco/elliptic-data-set) - see docs/external_validation.md
data/external/**/*.csv

# Run-output database from the bulk profile. Regenerated by `make run`,
# same class as case.duckdb and train.duckdb above.
data/bulk.duckdb
